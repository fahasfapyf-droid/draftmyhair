# Promo Code System

Admin-issued promo codes grant fixed credits. Codes are uppercase, one redemption per user, optionally capped by total usage and bounded by start/expiry dates. Redemptions are recorded and credit award is atomic.